---
const {frontmatter} = Astro.props
import { format, parseISO } from 'date-fns';

var dn = new Date();
var ds = format(dn, 'yyyy-MM-dd')
if (!!frontmatter.date) {
  ds = format(parseISO(frontmatter.date), 'yyyy-MM-dd')
}

---

<article>
  <header class="flex flex-col">
    <h1 class="mt-6 text-4xl font-bold tracking-tight text-zinc-800 dark:text-zinc-100 sm:text-5xl">{frontmatter.title}</h1>
    <time datetime="2022-09-05" class="order-first flex items-center text-base text-zinc-400 dark:text-zinc-500">
      <span class="h-4 w-0.5 rounded-full bg-zinc-200 dark:bg-zinc-500">
      </span>
      <span class="ml-3">{ds}</span>
    </time>
  </header>
  <div class="mt-8 prose  prose-slate  dark:prose-invert">
    <slot />
  </div>
</article>